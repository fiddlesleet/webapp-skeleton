{
  "_comment": "Use npm install package.json to install download all required modueles",
  "name": "HelloWorld",
  "description": "Skeleton for reviewing webapp structure",
  "dependencies": {
    "express": "*",
    "ejs": "*",
    "aws-sdk": "*"
  }
}
